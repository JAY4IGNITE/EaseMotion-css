# Live Demo Fix Submission

## Issue
[BUG] Live Demo link opens GitHub source instead of rendered page

## Goal
This submission provides a browser-rendered demo page so users can preview the animations directly instead of landing on raw source code.

## Files Included
- `demo.html`
- `style.css`
- `README.md`

## How it works
- `demo.html` displays a real webpage preview.
- `style.css` adds layout, gradients, cards, buttons, and animation examples.
- The page can be hosted with GitHub Pages or any static hosting service.

## Notes
- This folder is self-contained and placed only inside `submissions/`.
- It follows the repository’s contribution rule for isolated submissions.
- It is meant to support Issue 1 only.

## Preview
Open `demo.html` in a browser to test the live demo experience.